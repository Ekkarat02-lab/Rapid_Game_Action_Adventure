using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Heal_ltem : ItemData
{

    /*public PlayerStats playerStats;
    protected override void UseItem()  // Override ฟังก์ชันจากแม่แบบ
    {
        base.UseItem();  // เรียกฟังก์ชันจาก ItemData (แม่แบบ)
        if (isHealingItem)
        {
            Heal();
            Destroy(gameObject);
           
        }
       
    }
    public void Heal()
    {
        playerStats.CurrentHealth = playerStats.maxHealth;
    }*/
}
